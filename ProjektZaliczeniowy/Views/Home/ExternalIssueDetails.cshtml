@model ProjektZaliczeniowy.Models.Entities.ExternalIssue

@{
    ViewData["Title"] = "Szczegóły przyjęcia zewnętrznego";
}

<div class="container mt-5">
    <h1>Szczegóły przyjęcia zewnętrznego nr @Model.Id</h1>
    <p>Data przyjęcia: @Model.Date.ToString("yyyy-MM-dd HH:mm")</p>
    <p>Wprowadzający: @Model.User.Username</p>
    <p>Uwagi: @Model.Remarks</p>
    <hr />
    <h3>Produkty</h3>
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Nazwa produktu</th>
                <th>Ilość</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var detail in Model.Details)
            {
                <tr>
                    <td>@detail.Product.Name</td>
                    <td>@detail.Quantity</td>
                </tr>
            }
        </tbody>
    </table>
</div>
